/*import PropTypes from 'prop-types'*/
const Button = ({onClick}) => {
    /*const onAdd = (e) => {
        console.log('Button Pressed')
    }*/
    return (
        <button
            onClick={onClick}
            > Search
        </button>
    )
}

export default Button